version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:8080"
    env_file:
      - .env
    environment:
      - PORT=3001
      - MIMO_BASE_URL=https://aistudio.xiaomimimo.com
      - NODE_ENV=production
    restart: unless-stopped
